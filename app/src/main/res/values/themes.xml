<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.InfoTasks" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Mis Estilos -->
    <style name="btn">
        <item name="android:textSize">40dp</item>
    </style>
    <style name="textoIdentificarElemento">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">18dp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="textoInput">
        <item name="android:textSize">16dp</item>
        <item name="android:inputType">text</item>
        <item name="background">@drawable/border_input_text</item>
    </style>

    <style name="textoError">
        <item name="android:textSize">10dp</item>
        <item name="android:textColor">@color/rojoError</item>
        <item name="android:visibility">invisible</item>
    </style>

    <style name="btnFiltro">
        <item name="android:textSize">8dp</item>
        <item name="android:layout_weight">1</item>
        <item name="cornerRadius">0dp</item>
    </style>
</resources>